version: '3.8'
services:
  uptime-kuma:
    container_name: Uptime-Kuma
    image: louislam/uptime-kuma
    volumes:
        - /mnt/gluster/containers/uptime-kuma:/app/data
    ports:
        - 9011:3001
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints: [node.platform.os == linux]
    restart: unless-stopped
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.file-browser.entrypoints=https"
      - "traefik.http.routers.file-browser.tls=true"
      - "traefik.http.routers.file-browser.rule=Host(`uptime-kuma.hyvecloud.dev`)"
      - "traefik.http.routers.file-browser.service=uptime-kuma-uptime-kuma@docker"
networks:
  proxy:
    external: true
