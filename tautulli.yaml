version: "3.8"
services:
  tautulli:
    image: lscr.io/linuxserver/tautulli
    restart: unless-stopped
    environment:
      PUID: 1000
      PGID: 1000
      TZ: Europe/London
    volumes:
      - /mnt/gluster/containers/tautulli:/config
    ports:
      - 8686:8181
