version: "3.8"
services:
  bookstack:
    container_name: Bookstack
    image: lscr.io/linuxserver/bookstack
    restart: unless-stopped
    environment:
      PUID: 1000
      PGID: 1000
      APP_URL: https://docs.hyvecloud.dev
      DB_HOST: bookstack_db
      DB_PORT: 3306
      DB_USER: bookstack
      DB_PASS: "${DB_PASS}"
      DB_DATABASE: bookstackapp
    env_file:
      - .env
    volumes:
      - /mnt/gluster/containers/bookstack/data:/config
    ports:
      - 8001:80
    networks:
      - proxy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bookstack.entrypoints=https"
      - "traefik.http.routers.bookstack.tls=true"
      - "traefik.http.routers.bookstack.rule=Host(`docs.hyvecloud.dev`)"
      - "traefik.http.routers.bookstack.service=bookstack-bookstack@docker"
    depends_on:
      - bookstack_db
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints: [node.platform.os == linux]
  bookstack_db:
    container_name: BookstackDB
    image: lscr.io/linuxserver/mariadb
    restart: unless-stopped
    environment:
      PUID: 1000
      PGID: 1000
      MYSQL_ROOT_PASSWORD: "${DB_ROOT_PASS}"
      TZ: Europe/London
      MYSQL_DATABASE: bookstackapp
      MYSQL_USER: bookstack
      MYSQL_PASSWORD: "${DB_PASS}"
    env_file:
      - .env
    volumes:
      - /mnt/gluster/containers/bookstack/db_data:/config
    networks:
      - proxy
    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints: [node.platform.os == linux]
networks:
  proxy:
    external: true